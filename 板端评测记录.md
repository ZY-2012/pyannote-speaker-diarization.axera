# community-1 AX650 量化板端评测记录

评测日期：2026-09-08。量化工程：`pyannote_community1.AXERA/`（技术细节见其 README.md）。

## 1. 部署架构（为什么这样拆）

| 组件 | 载体 | 原因 |
|---|---|---|
| sincnet 首层 conv（80×251, s=10） | **NPU**：主机 im2col + MatMul | C=1 输入 Conv 在 NPU 上算错（check=2 xrun 8% 超差）；图内 Unfold tile 失败 |
| sincnet conv(80→60)/(60→60) + 3 个 MaxPool | **NPU** ×2 模型 | 多通道 conv/pool 验证准确 |
| InstanceNorm ×4 | 主机 FP32 | U16 量化域数值崩坏（原生算子 maxdiff 5.4；手工分解 cos 0.31） |
| 4 层 BiLSTM | **NPU** ×4 cell 模型（32 步展开块，FP32 状态跨块）+ 主机调度 | Pulsar2 不支持 LSTM 算子；c 状态 clamp ±8（tanh 饱和，近似误差 0.05） |
| FFN + powerset 解码 | 主机 FP32 | 3 个小 MatMul |
| fbank（kaldi 80 mel） | 主机 numpy | 与 torchaudio 对齐（FFT 差异 1e-6） |
| ResNet34 卷积栈 | **NPU** | 标准 conv/bn |
| TSTP 加权统计池化 + seg_1 | 主机 FP32 | 加权 mean/std 在 U16 崩坏（端到端 cos 0.876→0.997） |
| PLDA + AHC + VBx 聚类 | 主机 numpy/scipy | 纯算法 |

NPU 上运行：6 个 axmodel（seg A/B1/B2 + lstm cell ×4（可选，或 numpy LSTM）+ embedding 卷积栈）。
模型总大小 ≈ 15 + 28×3 + 5.6 + 若干 ≈ 100 MB（含 cell 权重）。

## 2. 精度对拍（板端 vs GPU FP32，真实音频）

| 环节 | cosine | maxdiff |
|---|---|---|
| sincnet A（MatMul+Abs+MaxPool） | 0.9995 | 0.005 |
| sincnet B1/B2 | 0.99999/0.99998 | 离群 1.8/2.8（norm 平滑后无影响） |
| sincnet features 端到端 | 0.99997 | 0.023 |
| LSTM（numpy 后端） | 精确（0 帧差） | — |
| LSTM（NPU cell 后端，clamp±8） | 60s DER 与 numpy 后端一致 | LSTM 输出 maxdiff ~0.2/layer |
| embedding 端到端 | 0.997 | 0.026 |

## 3. AliMeeting Eval 4 场（帧级评分器，collar ±0.125/±0.25）

| 系统 | ±0.125 | ±0.25 |
|---|---|---|
| **community-1 板端量化** | **20.86%** | **16.69%** |
| community-1 GPU FP32 | 19.14% | 14.90% |
| 量化损失 | **+1.72 pp** | **+1.79 pp** |
| 3D-Speaker +overlap | 18.51% | 13.99% |
| 3D-Speaker 默认 | 30.16% | 24.39% |

逐场（±0.125，board / GPU）：

| meeting | 重叠 | board | GPU | Δ |
|---|---|---|---|---|
| R8001_M8004 | 27% | 22.33% | 17.56% | +4.77 |
| R8003_M8001 | 17% | 13.31% | 12.19% | +1.12 |
| R8007_M8010 | 44% | 31.05% | 30.27% | +0.78 |
| R8007_M8011 | 23% | 14.61% | 13.83% | +0.78 |

## 3.5 AMI dev12（no collar，官方 NXT words 参考）

| 系统 | DER | miss | FA | conf |
|---|---|---|---|---|
| **community-1 板端量化** | **19.86%** | 12.91% | 2.87% | 4.08% |
| community-1 GPU FP32 | 19.96% | — | — | — |
| 量化损失 | **−0.10 pp**（无退化） | | | |
| 3D-Speaker +overlap | 22.25% | 12.53% | 4.50% | 5.23% |
| 3D-Speaker 默认 | 30.94% | — | — | — |

**英文 AMI 上量化几乎无损**（19.86% vs GPU 19.96%），仍领先 3D+overlap 2.4 pp。

## 4. 性能（AX650N，RTF = 纯推理/音频时长，不含模型加载）

| 配置 | RTF（加权） | 备注 |
|---|---|---|
| **step=2s + C++（8 线程）**（推荐） | **0.084** | 样例 120s 实测；30 分钟会议 0.119（含聚类） |
| step=2s + Python | 0.160 | 16 场实测 |
| step=1s + cell LSTM（Python） | 0.441 | AMI dev12 实测 |

分阶段（step=2s，每窗口）：分割 ~0.4s（前端 NPU + LSTM cell + FFN）、
嵌入 ~0.2s、聚类每场 ~30s。模型加载（约 5s，6 个 axmodel + 权重）不计入。

板端 vs GPU 整场 DER：9.05%（R8001_M8004）——长会议下聚类对嵌入扰动的
放大效应，逐场 0.8~4.8 pp 不等。

## 4. 性能（AX650N）

| 阶段 | 耗时/窗口（10s） | 后端 |
|---|---|---|
| 分割（前端+LSTM+FFN） | 0.33 s（cell） / 1.5 s（numpy LSTM） | NPU+CPU |
| 嵌入（3 个说话人槽 × 卷积栈） | ~0.05 s | NPU |
| 聚类 | 每场 ~30 s | CPU |

整场 30 分钟会议：cell 后端 ≈ 20 分钟，numpy LSTM 后端 ≈ 40 分钟。

## 6. 与 3D-Speaker 板端量化（Meeting-Summary demo）对比

对比对象：`3D-Speaker-Meeting-Summary/`（板端会议转录 demo 的说话人分割部分：
FSMN VAD + CAM++ 嵌入 + 谱聚类，均为 axmodel 量化；无重叠检测；谱聚类配置
mer_cos=0.8 / max 15 / min_cluster 4 / pval 0.012，同 GPU 3D默认）。
新增 `diar_only.py` 只跑分割部分并输出 RTTM。

| 系统 | AMI dev12 no-collar | AliMeeting ±0.125 | AliMeeting ±0.25 | RTF（纯推理） |
|---|---|---|---|---|
| **community-1 板端量化（step=2s）** | **20.06%** | **20.88%** | **17.26%** | 0.084（C++ 8 线程）/ 0.16（Python） |
| 3D-Speaker 板端量化（demo） | 29.72% | 29.34% | 24.36% | **0.046** |
| 差值（community-1 优势） | **+9.7 pp** | **+8.5 pp** | **+7.1 pp** | 3D 快 2.0×（vs C++） |

3D-Speaker 板端逐场（±0.125）：R8001 32.60% / R8003 20.68% /
R8007_M8010 42.96%（44% 重叠） / R8007_M8011 21.11%——高重叠会议是其软肋
（无重叠检测）。与 GPU 3D默认（30.16%）一致，说明其量化无损，差距是架构性的。

参考（GPU，记录值）：3D+overlap 22.25%（AMI）/ 18.51%、13.99%（AliMeeting）。

**板端对板端结论**：community-1 量化在三个口径上全面领先 3D-Speaker 板端量化
7.7~9.9 pp，但速度慢约 10 倍（RTF 0.441 vs 0.046）。若场景对实时性要求高
（<0.1 RTF），3D-Speaker 板端是更轻的选择；对准确率优先，community-1 明确胜出。

## 7. 提速优化（RTF 0.441 → 0.160）

瓶颈分析：pyannote 协议按 1s 步长滑动 10s 窗口（10 倍计算重叠），每窗口
0.2~0.4s（LSTM cell 串行链 0.15s + 嵌入 0.1s）。

| 优化 | 效果 |
|---|---|
| fbank 每窗口只算一次（3 槽位共享） | −90ms/窗口 |
| 全零掩码槽位短路（嵌入=偏置常数，精确） | −30ms/窗口 |
| LSTM cell 块 64→96 + gemm 融合 + 嵌入卷积栈每窗口只跑一次 | 串行链延迟为硬下限，块大小无增益 |
| **滑动步长 1s→2s**（`--step 2.0`） | **计算量减半，RTF 0.441→0.160** |

步长 DER 代价（GPU FP32 整场加权实测）：2s 几乎无损（ALI 18.84% vs 19.14% 甚至
略优，AMI +0.12pp）；2.5s 基本免费（ALI 18.89% / AMI 20.19%，+0.05/+0.11pp）；
3s 退化 0.5~0.7pp。**推荐 step=2s；追求速度可用 2.5s**（板端 R8001 实测
RTF 0.069 样例 / 0.088 整场，比 2s 快约 20~27%，该场 DER +1.4pp——
板端量化对窗口网格偏移比 GPU 敏感，逐场 0~1.5pp 浮动）。

step=2s 板端实测（RTF 0.159~0.162，纯推理）：

| 数据集 | board-step2 | GPU-FP32-step2 | 量化损失 |
|---|---|---|---|
| AMI dev12 no-collar | **20.06%** | 20.08% | −0.02 pp |
| AliMeeting ±0.125 | 20.88% | 18.84% | +2.04 pp |
| AliMeeting ±0.25 | 17.26% | 15.16% | +2.10 pp |

## 5. 结论（AliMeeting + AMI dev12 全部完成）

| 数据集 | 板端量化 | GPU FP32 | 量化损失 | 3D+overlap | 对 3D 优势 |
|---|---|---|---|---|---|
| AMI dev12 no-collar | **19.86%** | 19.96% | −0.10 pp | 22.25% | **+2.39 pp** |
| AliMeeting ±0.125 | 20.86% | 19.14% | +1.72 pp | 18.51% | −2.35 pp |
| AliMeeting ±0.25 | 16.69% | 14.90% | +1.79 pp | 13.99% | −2.70 pp |

- **英文 AMI**：量化几乎无损，明确领先 3D+overlap。
- **中文 AliMeeting**：量化损失 1.7~1.8 pp，落后 3D+overlap 2.4~2.7 pp
  （GPU FP32 本身也只落后 0.6~0.9 pp，中文非 community-1 主场）。
- 选型结论：**英文会议场景直接替换；中文场景需权衡**——community-1 是单模型
  部署（无需 FSMN VAD/CAM++/谱聚类/重叠检测四组件），工程复杂度远低于
  3D-Speaker；若要追平中文指标，可叠加 CAM++ 混合重标（LS-EEND 路线已探明该
  后处理 +2~5 pp）或在 PLDA 阈值/人数估计上轻量调参。
- RTF 0.441（cell 后端，纯推理，AX650N），单模型 ~100 MB 全链路可跑。
